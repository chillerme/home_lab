---
- hosts: all
  tasks:
  - name: Update all installed packages using dnf module
    dnf:
      name: '*'
      state: latest
      update_cache: yes
      update_only: yes
    register: dnf_update_status
    when: ansible_hostname not in [
      'hrtsofpiaapcon.sofpi.sofapps.com',
      'hrtsofpiaapeda.sofpi.sofapps.com',
      'hrtsofpiaaphub.sofpi.sofapps.com',
      'hrtsofpiaappsql.sofpi.sofapps.com'
    ]

  - name: Remove packates not needed anymore
    dnf:
      autoremove: yes

  - name: Reboot when packages were updated
    reboot:
      post_reboot_delay: 60
    when: dnf_update_status.changed

- hosts: apt
  tasks:
  - name: Update all installed packages using APT module
    apt:
      name: '*'
      state: latest
      update_cache: yes
      only_upgrade: yes
    register: apt_update_status

  - name: Remove packages not needed anymore
    apt:
      autoremove: yes

  - name: Reboot when packages were updated
    reboot:
      post_reboot_delay: 180
    when: apt_update_status.changed
